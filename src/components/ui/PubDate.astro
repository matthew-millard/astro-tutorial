---
import { formatDate } from "@utils/format-date";

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<p class="pub-date">{formatDate(date)}</p>

<style>
  .pub-date {
    font-size: var(--step--1);
  }
</style>
